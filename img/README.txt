Optional photography. Only ONE slot exists now: img/hero.jpg

It sits behind the home page hero, as texture behind the headline - not as
the subject. Wide crop, dark or mid-tone image works best (a round in
progress, the room mid-meeting, a competition table).

If the file is absent the hero is a solid dark block. Nothing looks broken
or empty either way - the hero carries its own text and stat bar.

2400x1200 or wider, under ~400KB.
